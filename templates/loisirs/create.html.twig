<!DOCTYPE html>
<html>
    <h1>Création d'un nouveau loisir</h1>
    {{ form_start(form, {'action': path('valid_loisir'), 'attr': {'novalidate': 'novalidate' } }) }}
    {{ form(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</html>