<!DOCTYPE html>
<html>
     <h1>Création d'une nouvelle expérience</h1>
    {{ form_start(form, {'action': path('valid_experiences'), 'attr': {'novalidate': 'novalidate' } }) }}
    {{ form(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</html>